/**
 * Created by vtk-anhlt166 on 9/20/21.
 */
function getIconWithType() {
    //console.log(from, to);

    // var radianAngle = bearing(from, to);
    // var degree = radianAngle/Math.PI*180; //convert to degree
    // if(type == MARKER_TYPE.SOS.type || type == MARKER_TYPE.BL.type || (battery <= 15 && battery > 0)){
    //     image = alarm;
    // } else if(type == MARKER_TYPE.RUN.type){
    //     image = startRun;
    // } else if(type == MARKER_TYPE.PARK.type){
    //     image = stop;
    // } else {
    //     image = point;
    // }
    return carRun;

}